import type theme from 'theme';

export const LABELS = ['Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun'];

export const GRADIENTS: (keyof typeof theme.gradients)[] = [
  'sunsetOrange',
  'green',
  'purple',
  'yellow',
  'sunsetOrange',
  'green',
  'purple',
];

export const HEIGHT = 220;
