import type {Size} from './types';

export const SIZE_VALUES: Record<Size, number> = {
  regular: 44,
  bigger: 100,
};

export const ICON_SIZE_VALUES: Record<Size, number> = {
  regular: 20,
  bigger: 45,
};
